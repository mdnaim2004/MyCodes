#include<bits/stdc++.h>
using namespace std;

int main(){
    int n, e;
    cin >> n >> e;

    while(e--){
        int a,b,c;
        cin >> a >> b >> c;
    }


    int q;
    cin >>q;

    while(q--){
        int x, y;
        cin >> x >> y;

    }
    return 0;
}